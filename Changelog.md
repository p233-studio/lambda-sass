# Changelog

## 2.1.1 (January 20, 2026)

Bug fixes:

- Fix typo in `__thread` function (`$_new_args` → `$_new-args`)

Performance:

- Optimize `some` and `every` with short-circuit evaluation

## 1.0.2 (November 28, 2020)

Added functions:

- `not-equal`
- `range`
- `conj`
- `minus`
- `multiply`
- `divide`

Updated functions:

- Rename `add` to `plus`
